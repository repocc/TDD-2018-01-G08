(ns estado.contador-test
  (:require
    [clojure.test :refer :all]
    [estado.contador :refer :all]
    [setup.reglas-ejemplo :as reglasEjemplo] :reload-all)
  (:import [estado.estado Estado]))

;TODO(Iván): Test agregarContador y procesarUnaRegla.
